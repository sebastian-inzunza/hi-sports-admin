import AppLayout from '@/components/layout/app'

const Reports = () => {
  return (
    // In this page the user can choose by date range to see the reports of the alerts
    // And also can export the data to a CSV file, use tailwindcss for the styles
    <div className="flex flex-col items-center justify-center w-full h-full"></div>
  )
}
Reports.Layout = AppLayout

export default Reports
